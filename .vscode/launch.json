{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug in Microsoft Edge",
      "type": "pwa-msedge",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/web",
      "preLaunchTask": "dev:server"
    }
  ]
}
